name: My Workflow

on: push

jobs:
  job1:
    runs-on: ubuntu-latest
    steps: 
      - name: Step 1
        run: echo "This is step 1"

      - name: Step 2
        if: contains(${{ github.event.head_commit.message }},'skip_test')
        run: echo "This is step 2 ${{ github.event.head_commit.message }}"

